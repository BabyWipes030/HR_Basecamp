length = float(input("Length: "))
width = float(input("Width: "))

def roomAreaCalculator(length, width):
    area = length * width
    return print(f"The Area of the Room: {area}")

roomAreaCalculator(length, width)
